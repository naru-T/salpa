% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/minimize_loss.R
\name{minimize_loss}
\alias{minimize_loss}
\title{Minimize loss}
\usage{
minimize_loss(
  lidar_footprints,
  input_rast,
  minimizing_method,
  target_variable,
  buf,
  crs_code,
  lidar_value,
  optimization_method = "ga",
  lower_bounds = c(-30, -30),
  upper_bounds = c(30, 30),
  pop_size = 50,
  max_iter = 100,
  parallel = FALSE,
  pso_params = NULL
)
}
\arguments{
\item{lidar_footprints}{sf object containing satellite LiDAR footprints}

\item{input_rast}{referenced raster object for extraction}

\item{minimizing_method}{character string for the minimizing method. Default is "euclidean".
Options are "dtw", "euclidean", "manhattan", "correlation", "area", "hausdorff".}

\item{target_variable}{character string for the target variable (min, max, mean, median, etc.). See exact_extract}

\item{buf}{numeric value for buf size. Default is 12.5}

\item{crs_code}{numeric value for CRS code}

\item{lidar_value}{name of the column containing LiDAR measurement values}

\item{optimization_method}{character string for optimization method. Options are "ga" (genetic algorithm) or "pso" (particle swarm optimization). Default is "ga".}

\item{lower_bounds}{numeric vector for lower bounds in optimization. Default is c(-30, -30)}

\item{upper_bounds}{numeric vector for upper bounds in optimization. Default is c(30, 30)}

\item{pop_size}{numeric value for population size (GA) or swarm size (PSO). Default is 50}

\item{max_iter}{numeric value for maximum iterations. Default is 100}

\item{parallel}{logical value for parallel processing. Default is FALSE}

\item{pso_params}{list of additional parameters for PSO. Default is NULL.
List can include: trace (logical), maxit (numeric), maxf (numeric), abstol (numeric), reltol (numeric), REPORT (numeric), trace.stats (logical)}
}
\value{
list with optimization results
}
\description{
Minimize loss
}
